# Frontend Placeholder

The active frontend for this repository lives at the root of the Next.js app (`app/`, `components/`, `lib/`). This directory is intentionally unused.

## Status
- Placeholder only. No build tooling or runtime code lives here.
- The active frontend stack is **React 18** + **TypeScript** via Next.js App Router.

## Where to Work Today
- Routes and pages: `app/`
- Shared UI components: `components/`
- Shared utilities: `lib/`
