openapi: 3.0.3
info:
  title: Firm Platform API
  description: API gateway documentation for platform capabilities.
  version: 1.0.0
servers:
  - url: https://api.example.com/v1
    description: Production (v1)
  - url: https://api.example.com/v2
    description: Production (v2)
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
paths:
  /forms:
    get:
      summary: List forms
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of forms
